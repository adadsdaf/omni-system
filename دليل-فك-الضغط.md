# دليل فك ضغط ملفات إتقان سوفت

## ملفات متاحة للتنزيل

| الملف | الحجم | الوصف |
|-------|-------|-------|
| `itqan-full-project.tar.gz` | ~192MB | المشروع الكامل (كود + جميع الحزم) — **الموصى به** |
| `itqan-source-code.tar.gz` | ~104MB | الكود المصدري فقط (بدون node_modules) |
| `itqan-packages.tar.gz` | ~85MB | مخزن الحزم pnpm فقط |

---

## متطلبات النظام (قبل التثبيت)

| الأداة | الإصدار المطلوب | رابط التنزيل |
|--------|----------------|-------------|
| Node.js | **v24.x** | https://nodejs.org |
| pnpm | **v10.x** | `npm install -g pnpm@latest` |
| Python | **3.11+** (لبناء better-sqlite3) | https://python.org |
| node-gyp | أحدث إصدار | `npm install -g node-gyp` |
| Visual C++ Build Tools | (Windows فقط) | https://visualstudio.microsoft.com/downloads/ |
| Xcode Command Line Tools | (macOS فقط) | `xcode-select --install` |

---

## طريقة فك الضغط

### على Linux / macOS

```bash
# فك ضغط المشروع الكامل
tar -xzf itqan-full-project.tar.gz -C ./itqan-pos

# الدخول إلى المجلد
cd itqan-pos
```

### على Windows

**الطريقة 1 — باستخدام 7-Zip (موصى به):**
1. نزّل وثبّت [7-Zip](https://www.7-zip.org)
2. اضغط بزر الماوس الأيمن على الملف
3. اختر: **7-Zip ← Extract Here**

**الطريقة 2 — باستخدام PowerShell:**
```powershell
tar -xzf itqan-full-project.tar.gz -C .\itqan-pos
cd itqan-pos
```

**الطريقة 3 — باستخدام WSL (Windows Subsystem for Linux):**
```bash
tar -xzf itqan-full-project.tar.gz -C ./itqan-pos
cd itqan-pos
```

---

## بعد فك الضغط: تثبيت التبعيات

### إذا استخدمت `itqan-full-project.tar.gz`

الحزم موجودة بالفعل، فقط قم بإعادة بناء الوحدات الأصلية:

```bash
# 1. إعادة بناء better-sqlite3 (ضرورية على كل جهاز)
pnpm rebuild better-sqlite3

# 2. التحقق من نجاح البناء
node -e "require('better-sqlite3')(':memory:')" && echo "OK"
```

### إذا استخدمت `itqan-source-code.tar.gz`

يجب تثبيت الحزم من الشبكة:

```bash
# تثبيت كل الحزم
pnpm install

# إعادة بناء الوحدات الأصلية
pnpm rebuild better-sqlite3
```

---

## تشغيل المشروع

### وضع التطوير (Development)

```bash
# نافذة 1: تشغيل الخادم (API Server)
cd artifacts/api-server
PORT=8080 NODE_ENV=development node build.mjs && PORT=8080 node --enable-source-maps dist/index.mjs

# نافذة 2: تشغيل الواجهة (Frontend)
PORT=5000 BASE_PATH=/ artifacts/pos-system/node_modules/.bin/vite \
  --config artifacts/pos-system/vite.config.ts --host 0.0.0.0
```

ثم افتح المتصفح على: **http://localhost:5000**

### بناء تطبيق سطح المكتب (Electron)

```bash
# بناء كل شيء + إنشاء ملف التثبيت
node scripts/build-desktop.mjs
```

الملف الناتج سيكون في `electron-app/dist/`

---

## بيانات الدخول الافتراضية

| المستخدم | كلمة المرور | الدور |
|----------|------------|-------|
| `admin` | `admin123` | مدير النظام |
| `cashier` | `cashier123` | كاشير |

---

## حل المشاكل الشائعة

### ❌ خطأ: `null bytes in environment variable`

**السبب:** ملف `.npmrc` يحتوي على `blockExoticSubdeps=false` مشفراً بـ UTF-16.

**الحل:**
```bash
# احذف الملف وأنشئه من جديد
echo "auto-install-peers=false" > .npmrc
echo "strict-peer-dependencies=false" >> .npmrc
```

### ❌ خطأ: `Cannot find module 'better-sqlite3'` أو `NODE_MODULE_VERSION mismatch`

**السبب:** الوحدة الأصلية (native module) مبنية لإصدار Node.js مختلف.

**الحل:**
```bash
pnpm rebuild better-sqlite3
```

### ❌ خطأ: `node-gyp` أو `Python not found` (عند بناء better-sqlite3)

**على Windows:**
```powershell
npm install -g windows-build-tools
npm install -g node-gyp
```

**على macOS:**
```bash
xcode-select --install
```

**على Linux (Ubuntu/Debian):**
```bash
sudo apt-get install -y build-essential python3
```

### ❌ الواجهة تفتح لكن لا تتصل بالخادم

تأكد أن الخادم يعمل على المنفذ `8080` والواجهة على `5000`.
الواجهة تُوجّه طلبات `/api/*` تلقائياً إلى `http://localhost:8080`.

### ❌ خطأ `PORT environment variable required`

تأكد من تمرير متغيرات البيئة:
```bash
PORT=8080 node dist/index.mjs       # للخادم
PORT=5000 BASE_PATH=/ vite dev       # للواجهة
```

---

## هيكل المشروع

```
itqan-pos/
├── artifacts/
│   ├── api-server/          ← الخادم (Express + SQLite)
│   │   ├── src/             ← كود TypeScript
│   │   ├── dist/            ← البناء المُجمَّع (بعد البناء)
│   │   └── data/pos.db      ← قاعدة البيانات (تُنشأ تلقائياً)
│   └── pos-system/          ← الواجهة (React + Vite)
│       └── src/
├── lib/
│   ├── api-spec/            ← مواصفة OpenAPI
│   ├── api-client-react/    ← Hooks مُولَّدة تلقائياً
│   └── api-zod/             ← مخططات Zod
├── electron-app/            ← تطبيق سطح المكتب
├── scripts/                 ← سكريبتات البناء
├── pnpm-workspace.yaml      ← إعدادات مساحة العمل
└── pnpm-lock.yaml           ← قفل إصدارات الحزم
```

---

## ملاحظات مهمة

- **قاعدة البيانات** تُنشأ تلقائياً في `artifacts/api-server/data/pos.db` عند أول تشغيل
- **جلسات المستخدمين** مخزنة في الذاكرة — إعادة تشغيل الخادم تُلغي جميع الجلسات
- **الطباعة المباشرة** تتطلب وجود طابعة متصلة بالشبكة أو الجهاز
- تأكد من عدم وجود نسختين من `blockExoticSubdeps` في `.npmrc`

---

*إتقان سوفت — نظام نقطة المبيعات المتكامل*
